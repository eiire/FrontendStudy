/**
 * @param {Number} a Первое слагаемое
 * @param {Number} b Второе слагаемое
 * @returns {Number}
 */
module.exports = function (a, b) {
    return Number(a) + Number(b);
};

var my_check = function (a, b) {
    return Number(a) + Number(b);
};

// console.info(my_check(5, 6));
